{
  "name": "viser4d-runtime",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "generate-runtime-messages": "node generate-runtime-messages.mjs",
    "build": "node build-runtime.mjs",
    "typecheck": "node generate-runtime-messages.mjs && tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.10",
    "typescript": "^5.9.2"
  }
}
